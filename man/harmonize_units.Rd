% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonize_units.R
\name{harmonize_units}
\alias{harmonize_units}
\title{Harmonize parsed value/unit columns to canonical units}
\usage{
harmonize_units(
  df,
  columns = c("territorial_footprint", "territorial_boundary", "footprint_percapita",
    "boundary_percapita"),
  skip_manual_review = TRUE,
  skip_non_physical = TRUE,
  keep_components = FALSE
)
}
\arguments{
\item{df}{A data frame that already contains parsed value/unit columns,
typically produced by \code{\link[=separate_unit_value2]{separate_unit_value2()}}.}

\item{columns}{Character vector with base column names to harmonize.
Defaults to \code{c("territorial_footprint", "territorial_boundary", "footprint_percapita", "boundary_percapita")}.}

\item{skip_manual_review}{Logical. If \code{TRUE}, rows flagged by
\verb{\{column\}_needs_manual_review} are not harmonized.}

\item{skip_non_physical}{Logical. If \code{TRUE}, rows flagged by
\verb{\{column\}_is_non_physical} are not harmonized.}

\item{keep_components}{Logical. If \code{TRUE}, adds parsed harmonization helper
columns such as multiplier, family, and denominator.}
}
\value{
The input data frame with new columns appended after each base column:
\itemize{
\item \verb{\{column\}_value_harmonized}
\item \verb{\{column\}_unit_canonical}
\item \verb{\{column\}_harmonized}
\item \verb{\{column\}_harmonization_failed}
\item \verb{\{column\}_harmonization_note}
}

If \code{keep_components = TRUE}, also adds:
\itemize{
\item \verb{\{column\}_unit_family}
\item \verb{\{column\}_unit_substance}
\item \verb{\{column\}_unit_denominator}
\item \verb{\{column\}_unit_multiplier}
}
}
\description{
\code{harmonize_units()} is designed to run after \code{\link[=separate_unit_value2]{separate_unit_value2()}}. It
takes the parsed \verb{\{column\}_value} and \verb{\{column\}_unit} columns, and creates
harmonized value/unit columns when a safe dimensional conversion is possible.
}
\details{
The function is intentionally conservative:
\itemize{
\item it skips rows flagged for manual review by default,
\item it skips non-physical indicators by default,
\item it rescales only when the unit family is clearly compatible,
\item it preserves semantic distinctions such as \code{CO2} vs \code{CO2eq}, \code{N} vs \code{N-eq},
and \code{P} vs \code{P-eq}.
}

Canonical internal targets used by this function are:
\itemize{
\item mass-like quantities -> metric tonnes (\code{t}) while preserving the substance
suffix, for example \code{tCO2eq}, \code{tN}, \code{tP}, \code{tC},
\item volume-like quantities -> cubic metres (\code{m3}),
\item area-like quantities -> hectares (\code{ha}).
}

Denominators such as \verb{/yr}, \verb{/cap}, and \verb{/cap/yr} are preserved.
}
\examples{
data("Nitrogen_Data")
x <- separate_unit_value2(Nitrogen_Data)
x2 <- harmonize_units(x)

}
\author{
Derek Corcoran \href{mailto:derek.corcoran.barrios@gmail.com}{derek.corcoran.barrios@gmail.com}

Giorgia Graells \href{mailto:gygraell@gmail.com}{gygraell@gmail.com}
}
