% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pb_cartogram.R
\name{pb_cartogram}
\alias{pb_cartogram}
\title{Planetary-boundary cartogram for a selected boundary}
\usage{
pb_cartogram(
  PB_df,
  world_sf,
  boundary = "Nitrogen",
  count_mode = c("weight_area", "overshoot", "log1p"),
  crs_equal_area = 8857,
  eps = 1e-09,
  top_n = 15,
  with_ties = TRUE,
  viridis_option = "H",
  title = NULL
)
}
\arguments{
\item{PB_df}{A data frame/tibble with at least columns \code{location_name},
\code{boundary_studied}, and \code{overshoot} (coercible to numeric).}

\item{world_sf}{An \code{sf} object of country polygons with a \code{location_name}
column matching \code{PB_df$location_name}.}

\item{boundary}{Character. The planetary boundary to filter to; matched against
\code{PB_df$boundary_studied}. Default is \code{"Nitrogen"}.}

\item{count_mode}{Character. How to compute the cartogram resizing variable:
\describe{
\item{\code{"weight_area"}}{Area-weighted allocation of total map area based on
\code{overshoot + eps} (so all countries have non-zero weight).}
\item{\code{"overshoot"}}{Use \code{overshoot} directly as the cartogram count.}
\item{\code{"log1p"}}{Use \code{log1p(overshoot)} as the cartogram count.}
}}

\item{crs_equal_area}{Integer or character. CRS passed to \code{\link[sf:st_transform]{sf::st_transform()}}
for area computations. Defaults to \code{8857}.}

\item{eps}{Numeric. Small constant added to overshoot when \code{count_mode = "weight_area"} to avoid zero weights. Default \code{1e-9}.}

\item{top_n}{Integer. Number of top countries (by \code{overshoot}) to label.
Default \code{15}.}

\item{with_ties}{Logical. Passed to \code{\link[dplyr:slice]{dplyr::slice_max()}} when selecting top
labels. Default \code{TRUE}.}

\item{viridis_option}{Character. Option passed to
\code{\link[ggplot2:scale_viridis]{ggplot2::scale_fill_viridis_c()}}. Default \code{"H"}.}

\item{title}{Character or \code{NULL}. Plot title. If \code{NULL}, a title is generated
as \code{"<boundary> (<count_mode>)"}.}
}
\value{
A named list with:
\describe{
\item{\code{carto_sf}}{\code{sf} object of cartogram geometries.}
\item{\code{top_labels_sf}}{\code{sf} object of centroid points for labeled countries.}
\item{\code{plot}}{A \code{ggplot} object of the cartogram map.}
\item{\code{data_joined}}{Pre-cartogram \code{sf} with \code{overshoot} joined and cleaned.}
\item{\code{data_ea}}{Equal-area projected \code{sf} with \code{area_m2} and \code{count_carto}.}
}
}
\description{
Build a Dorling-style area cartogram (via \pkg{cartogramR}) where each country
is resized according to a selected planetary-boundary metric (overshoot or a
transformation thereof), and returns the cartogram geometry, label positions,
a \pkg{ggplot2} map, and intermediate joined/projected data.
}
\details{
The function joins \code{PB_df} to \code{world_sf} by \code{location_name}, replaces missing
overshoot values with 0, clips negative overshoot to 0, projects to an equal-area
CRS to compute polygon areas, constructs a cartogram "count" variable based on
\code{count_mode}, computes the cartogram, and labels the top \code{top_n} countries by
original \code{overshoot} value.

Label placement uses \code{\link[sf:geos_unary]{sf::st_centroid()}} on the cartogram geometries for the
top \code{top_n} locations (by original \code{overshoot}). For multipart geometries or
very irregular polygons, centroids may fall outside the intended visual region;
consider alternatives such as \code{\link[sf:geos_unary]{sf::st_point_on_surface()}} if needed.
}
\examples{
\dontrun{
data(PBData)
data(World)
res <- pb_cartogram(
  PB_df = PB,
  world_sf = world,
  boundary = "Climate change",
  count_mode = "overshoot"
)

res$plot
}

}
