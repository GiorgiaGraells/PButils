% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/separate_unit_value2.R
\name{separate_unit_value2}
\alias{separate_unit_value2}
\title{Separate values and units from one or more dataframe columns}
\usage{
separate_unit_value2(
  df,
  columns = c("territorial_footprint", "territorial_boundary", "footprint_percapita",
    "boundary_percapita"),
  add_flags = TRUE,
  keep_cleaned_input = FALSE
)
}
\arguments{
\item{df}{A dataframe containing the columns to separate.}

\item{columns}{Character vector with the names of columns to separate.
Defaults to \code{c("territorial_footprint", "territorial_boundary", "footprint_percapita", "boundary_percapita")}.}

\item{add_flags}{Logical. If \code{TRUE}, adds diagnostic flag columns.}

\item{keep_cleaned_input}{Logical. If \code{TRUE}, adds a \verb{\{column\}_cleaned} column.}
}
\value{
The same dataframe with new columns appended after each parsed column.

Added columns per input column:
\itemize{
\item \verb{\{column\}_value}: numeric value when a single value can be parsed.
\item \verb{\{column\}_unit_raw}: extracted raw unit text.
\item \verb{\{column\}_unit}: harmonized unit text.
\item \verb{\{column\}_cleaned}: cleaned source string used for parsing (optional).
\item \verb{\{column\}_is_range}: whether the entry looks like a numeric range.
\item \verb{\{column\}_has_context}: whether the extracted unit contains likely commentary.
\item \verb{\{column\}_is_non_physical}: whether the unit looks like an index / indicator.
\item \verb{\{column\}_unit_harmonized}: whether the unit changed during harmonization.
\item \verb{\{column\}_needs_manual_review}: whether the entry should be reviewed manually.
}
}
\description{
Enhanced version of \code{separate_unit_value()} that:
\itemize{
\item cleans common formatting problems before parsing,
\item extracts a numeric value when possible,
\item keeps both raw and harmonized unit text,
\item adds flags for ranges, context text, non-physical indicators, and review needs.
}
}
\details{
This function is intentionally conservative. It harmonizes units when the
equivalence is reasonably safe (for example \verb{m³} -> \code{m3}, \code{tons} -> \code{t},
\verb{/year} -> \verb{/yr}, \code{CO2-eq} -> \code{CO2eq}), but it does not attempt to resolve
ranges or contextual commentary into a single numeric value.
}
\examples{
data("Nitrogen_Data")
separate_unit_value2(df = Nitrogen_Data)
separate_unit_value2(
  df = Nitrogen_Data,
  columns = c("global_limit_considered", "boundary_percapita")
)

}
\author{
Derek Corcoran \href{mailto:derek.corcoran.barrios@gmail.com}{derek.corcoran.barrios@gmail.com}

Giorgia Graells \href{mailto:gygraell@gmail.com}{gygraell@gmail.com}
}
